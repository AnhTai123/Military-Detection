# Core
numpy
opencv-python
pillow

# Tracking
supervision>=0.18.0

# Detector (GroundingDINO via HuggingFace)
transformers>=4.40.0
torch

# VLM (default: CLIP — lightweight)
open_clip_torch

# VLM (optional: Qwen2.5-VL — heavier, only if --vlm qwen)
# qwen-vl-utils
# accelerate
