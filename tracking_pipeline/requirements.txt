# Core
numpy
opencv-python
pillow

# Tracking
supervision>=0.18.0

# Detector: YOLO (mặc định)
ultralytics>=8.0.0

# Detector / VLM: GroundingDINO via HuggingFace
transformers>=4.40.0
torch

# VLM (tùy chọn: CLIP — rất nhẹ)
open_clip_torch

# VLM (tùy chọn: Qwen2.5-VL — nặng hơn, chỉ khi --vlm qwen)
# qwen-vl-utils
# accelerate
